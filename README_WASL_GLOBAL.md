# WASL – Global Level Base

Added:
- Clean architecture starter folders (core/features/shared)
- Dio API client starter: `lib/core/network/api_client.dart`
- Base URL config: `lib/core/network/env.dart`

Run:
```bash
flutter clean
flutter pub get
flutter run
```
