/// تنزيل ملفّ نصّيّ — التنفيذ الافتراضيّ.
///
/// على غير الويب لا يوجد «تنزيل»؛ فيُعاد `false` ليعرض المستدعي بديلًا
/// (نسخٌ إلى الحافظة). والصمت هنا أسوأ: زرٌّ يُضغط ولا يحدث شيء.
Future<bool> downloadText(String filename, String content) async => false;
