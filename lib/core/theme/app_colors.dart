import 'package:flutter/material.dart';

class AppColors {
  // ✅ Official brand palette (as provided)
  static const Color primary = Color(0xFF2DB87A); // Primary Green
  static const Color navy = Color(0xFF1A2744); // Navy Blue
  static const Color accent = Color(0xFFD4A843); // Gold
  static const Color cream = Color(0xFFF7F5F0); // Cream

  // Semantic
  static const Color error = Color(0xFFE53935);

  // Light surfaces
  static const Color backgroundLight = cream;
  static const Color surfaceAlt = Color(0xFFF1F3F4);
  static const Color border = Color(0xFFE6E8EB);

  // Text (light)
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);

  // Dark surfaces
  static const Color backgroundDark = Color(0xFF0B1211);
  static const Color surfaceDark = Color(0xFF0F1A18);
  static const Color surfaceDark2 = Color(0xFF12201E);
  static const Color borderDark = Color(0xFF22302E);
}
