<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ═══════════════════════════════════════════════════════════════════
         الأذونات
         ═══════════════════════════════════════════════════════════════════
         الكاميرا والموقع تأتيان من بيانات الإضافات (mobile_scanner و
         geolocator) وتُدمج تلقائيًّا. وتُذكر هنا **صراحةً** كي تُقرأ في
         مراجعةٍ واحدة: من يقرأ هذا الملفّ يجب أن يعرف ما يطلبه التطبيق.

         ولا نطلب الموقع في الخلفية (ACCESS_BACKGROUND_LOCATION): نبضُ موقع
         السائق يقع والتطبيق مفتوح، وطلبُ إذن الخلفية يُراجَع في المتجر
         مراجعةً خاصّة ويُرفض إن لم يكن ضروريًّا. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- الكاميرا ليست شرطًا للتشغيل: تطبيق العميل لا يمسح شيئًا، ومن غير
         هذا السطر يختفي التطبيق من متجر الأجهزة التي لا كاميرا فيها. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- ═══════════════════════════════════════════════════════════════════
         ما يُنادى من تطبيقاتٍ أخرى
         ═══════════════════════════════════════════════════════════════════
         **بلا هذا القسم تفشل أزرار الاتصال والملاحة صامتةً على أندرويد ١١
         فما فوق**: النظام يُخفي التطبيقات الأخرى عن تطبيقنا، فيعيد
         `canLaunchUrl` كذبًا ولا يُرفع أيّ خطأ — يضغط السائق «اتّصال» فلا
         يحدث شيء. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
