# خارطة الطريق

> آخر تحديث: ٢٠٢٦-٠٨-٢٥

## الاختبارات

```bash
supabase/tests/run.sh   # ٢٢٧ اختبار قاعدة — على Postgres محلّيّ، ويتراجع بعده
flutter test            # ٦٩ اختبارًا — النماذج، والأخطاء، والعرض، والجرد، والمهامّ، وCSV
```

## المبدأ الحاكم

**لا Prototype ولا MVP مبسَّط.** البنية والوظائف الأساسية أوّلًا، والشكل بعدها.
وكل ما يُعلَن «مبنيًّا» هنا مُختبَرٌ فعلًا — لا مكتوبًا فحسب.

| المرحلة | الحالة |
|---|---|
| ١ — البنية: المخطّط والصلاحيات والمحرّكات | ✅ مبنيّ ومختبَر |
| ٢ — لوحة الإدارة | 🟡 قيد التنفيذ |
| ٣ — تطبيق العميل | 🟡 مسار الطلب والتتبّع مبنيّان |
| ٤ — تطبيق السائق والإسناد | ✅ مبنيّ ومختبَر |
| ٥ — تشغيل المغسلة والباركود | ✅ مبنيّ ومختبَر |
| ٦ — المدفوعات (ربط Moyasar) | ⬜ |
| ٧ — الخرائط وتخطيط المسارات | ⬜ |
| ٨ — الإشعارات (ربط المزوّد) | ⬜ |
| ٩ — التحليلات | ⬜ |
| ١٠ — الأمان والاختبار والنشر | ⬜ |

---

## المرحلة ٢ — لوحة الإدارة

### ما أُنجز

| البند | |
|---|---|
| **قائمة الطلبات** | ترشيح بمجموعاتٍ تشغيلية، وبحثٌ بالرقم أو الباركود |
| **تفصيل الطلب** | البنود والمبالغ والسجلّ، ونقلُ الحالة بأزرارٍ تُقرأ من القاعدة |
| **الموظّفون** | إسناد الأدوار وسحبها — بالبحث عن حسابٍ قائم لا بإنشائه |
| **المواعيد والطاقة** | ساعات العمل، وسقوف الفتحة واليوم، **ومعاينة حيّة للفتحات** |
| **الكوبونات** | الحدود والصلاحية والنطاق، وشريطُ استهلاكٍ يُرى قبل أن يُفاجئ |
| **التقارير** | ملخّص ورسمٌ يوميّ ومزيج خدمات، **وزمن كل مرحلة من سجلّ الأحداث** |
| **رسائل العميل** | قوالب لكل مرحلة بمتغيّرات ومعاينة حيّة، وتنبيهٌ للمراحل الصامتة |
| أساس Flutter بأربع نكهات | `AppFlavor` + أربعة مداخل، والنكهة تُثبَّت وقت البناء |
| الاتصال بـSupabase | `Db` + مترجم أخطاء عربيّ |
| الجلسة والأدوار | `SessionService` — ينادي `ensure_profile` ويقرأ الأدوار والفروع |
| شاشة الدخول | جوال (OTP) + بريد، والأوّل ينتظر تفعيل المزوّد |
| هيكل اللوحة | شريط جانبيّ على العريض، سفليّ على الضيّق، ومبدّل فروع |
| **لوحة اليوم** | طلبات اليوم، الإيراد، متوسّط الطلب، المتأخّر، وأين كلّ طلب |
| **الخدمات والأسعار** | إضافة وتعديل وإيقاف — الوعد الأساسيّ للنظام |
| **رسوم التوصيل** | الاستراتيجية والرسوم والشرائح، **ومعاينة حيّة** |

### ما بقي في هذه المرحلة

- [ ] **مناطق التوصيل (Geofencing)** — رسم المضلّعات على خريطة
- [ ] **خريطة السائقين** — الحالة والموقع لحظيًّا
- [ ] **تصدير PDF** — الحالي CSV؛ والفاتورة المطبوعة تحتاج `pdf` و`printing`

---

---

## المرحلة ٣ — تطبيق العميل

### ما أُنجز

| البند | |
|---|---|
| **الكتالوج** | بالأسعار **النافذة في الفرع** لا سعر المغسلة |
| **السلّة** | كمّيات بوحدةٍ لكل خدمة، وحدٌّ أدنى يُنبَّه عنده لا عند الإرسال |
| **إتمام الطلب** | عنوان (بوضع الزائر)، وموعدٌ من المتاح فعلًا، وكوبون، ودفع، وملخّص |
| **التتبّع** | خطٌّ زمنيّ كاملٌ من `order_events`، والفاتورة |
| **سجلّ الطلبات** | القائمة، والدخول إلى تتبّع أيٍّ منها |

### ما بقي

- [ ] **اختيار الموقع على خريطة** — الآن إحداثيّات نصّيّة
- [ ] **رفع صور القطع/البقع** عند الطلب
- [ ] **الدفع بالبطاقة** — يحتاج ربط Moyasar
- [ ] **التقييم بعد التسليم**
- [ ] **نقاط الولاء** في الواجهة (المحرّك مبنيّ)

---

## المرحلة ٤ — تطبيق السائق والإسناد

### ما أُنجز

| البند | |
|---|---|
| **حارس الإسناد** | الإسناد فعلُ إدارة، والمسنَد إليه **سائقٌ في فرع الطلب** فعلًا، وسقفُ مهامٍّ يمنع تحميل واحدٍ وتفريغ الباقي |
| **رمز التسليم** | يُولَّد بمحفّزٍ عند الخروج، يُخزَّن مُجزَّأً بـsha256 بمِلحٍ لكل طلب، بصلاحيةٍ وسقفِ محاولات — **ولا يُقرأ من الواجهة إطلاقًا** |
| **الرمز في رسالة العميل** | `{رمز_التسليم}` متغيّرٌ في القوالب — **وللعميل وحده**، فلا يصل من يُفترض أن يُتحقَّق منه |
| **الإثبات** | `complete_pickup` و`complete_delivery`: الإثبات والانتقال في نداءٍ واحد، بموقع من نفّذه |
| **موقع السائق** | `driver_locations` — صفٌّ يُحدَّث لا سجلٌّ يتراكم، بإحداثيّاتٍ مشتقّة، يقرؤه من يشغّل الفرع |
| **تطبيق السائق** | قائمةٌ واحدة للاستلام والتسليم بترتيب الموعد، وتبديلُ التفرّغ بنبضٍ بتواترٍ من القاعدة |
| **شاشة المهمّة** | العنوان والملاحة والاتصال، ومبلغُ التحصيل عند التسليم، وزرُّ خطوةٍ كبير |
| **الإسناد في اللوحة** | من تفصيل الطلب — سائق الاستلام وسائق التسليم، وبجانب كل اسمٍ حِملُه الآن |
| **إعدادات التسليم** | الرمز وطولُه ومدّته ومحاولاته، وسقفُ المهامّ، ونبضُ الموقع — كلُّها من اللوحة |

**وثغرةٌ أُغلقت في الطريق**: `auth_branch_ids()` كانت تعطي السائق فرعَه، فكان
يقرأ **كلَّ طلبات الفرع** بأسماء أصحابها وعناوينهم وهواتفهم لا المسنَدَ إليه
وحده. تسريبٌ لا يظهر في شاشة (التطبيق يرشّح بـ`driver_id` فتبدو القائمة
صحيحة). واستُثني دورُ السائق منها، وحقُّه في طلبه مذكورٌ صراحةً في كل سياسة.

### ما بقي

- [ ] **خريطة السائقين في اللوحة** — البيانات جاهزة (`driver_locations`)، والخريطة لم تُرسم
- [ ] **صورةُ إثبات** عند الباب — الحقل `photo_urls` موجود، والرفع لم يُبنَ
- [ ] **توقيع العميل** — الحقل `signature_url` موجود
- [ ] **تخطيط المسار** لعدّة مهامّ (المرحلة ٧)

---

## المرحلة ٥ — تشغيل المغسلة والباركود

### ما أُنجز

| البند | |
|---|---|
| **توليد الباركود في القاعدة** | `WSL-{رقم الطلب}` عند الإرسال، و`WSL-10042-01` لكل قطعة — بمُشغِّلٍ لا بالتطبيق |
| **حلّ الرمز** | `resolve_barcode()` — ملصق الكيس وملصق القطعة يؤدّيان إلى الطلب نفسه، ورمزٌ مجهول يعطي صفر صفوف لا خطأً |
| **مراحل القطع** | `sync_garment_stages()` — القطع تتبع الطلب داخل المغسلة وتقف عند آخر مرحلةٍ داخلية |
| **خطّ التشغيل** | لوحةٌ بسبع مراحل **بترتيب العمل لا أبجديًّا**، وعددُ كلٍّ منها باستعلامٍ واحد |
| **المسح** | كاميرا (`mobile_scanner`) وضوءٌ كشّاف، **وحقلُ إدخالٍ يدويٍّ دائم** لا احتياطيّ |
| **الاستقبال والجرد** | تسجيل القطع بالعيوب، ورمز QR للطلب، **ومقارنةُ المطلوب بالمجرود صراحةً** |
| **نقل المرحلة** | الأزرار تُقرأ من `order_transitions` — لا مسار مكتوبٌ في الشاشة |

**الفارق يُعلَن ولا يُطمس**: «طلب ٣ وجُرِد ٤» هو ما تُحسم به الخلافات بعد
التسليم. وبنود الوزن والسلّة لا تُقارَن بالقطع — ستّة كيلوغرامات ليست ستّ قطع،
ومقارنتها تُنتج إنذارًا كاذبًا يُدرَّب الموظّف على تجاهله.

### ما بقي

- [ ] **طباعة الملصقات** على طابعة حرارية (الرمز يُعرض ويُمسح، ولا يُطبع بعد)
- [ ] **صور القطع** — الحقل `photo_urls` موجود، والرفع لم يُبنَ

---

## المصادر المنقول عنها

| المستودع | ما يُنقل منه | ملاحظة |
|---|---|---|
| **`zadgo2`** | الأساس: ٤٩ ألف سطر، أربع شاشات أدوار، باركود، Moyasar، خرائط، تقارير | **لا يُكتب فيه حرف** |
| `zol` | نمط `AppRole` والنكهات، وMoyasar | مرجع |
| `taxi-taiba` | قواعد الأمان، واستعلام الجيوهاش | مرجع؛ **لا تطبيق سائق فيه** |
| `zadam` | Next.js + Supabase + RLS | مرجع للوحة ويب |

**عن المرحلة ٤**: كانت الخطة الاعتماد على «تاكسي طيبة» موصِّلًا، فتبيّن أنه
**تطبيق راكب ولا تطبيق سائق فيه** — وثائقه تقولها: «ولا شاشة تسجيل للسائق
بعد». فبُني طرفُ السائق هنا.

---

## معايير القبول — ما يُرفض به العمل

قبل أن تُعلَن أي مرحلة «منتهية»:

1. **لا سعر ولا رسم ولا قاعدة عمل في الشيفرة.** كلّها صفوفٌ تعدّلها الإدارة.
2. **لا بيانات وهمية بديلًا عن وظيفة.** شاشة «قيد التجهيز» أصدق من شاشةٍ
   تبدو عاملةً ببيانات ملفَّقة.
3. **الحراسة في القاعدة لا في الشاشة.** كل قيدٍ يجب أن يصمد أمام حزمةٍ معدَّلة.
4. **لا شيفرة تُسلَّم بلا تشغيل.** `flutter analyze` نظيف، والبناء ينجح،
   والشاشة تُفتح فعلًا وتُصوَّر.
5. **رسالة خطأٍ لكل مسار فشل.** بالعربية، ومفهومة، ولا تُفشي بنية.
